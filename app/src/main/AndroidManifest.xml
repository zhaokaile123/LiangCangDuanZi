<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atguigu.com.liangcangduanzi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.Type_itemsActivity" />
        <activity android:name=".activity.Gift_itemsActivity" />
        <activity android:name=".activity.LoginActiviy" />
        <activity android:name=".activity.Special_WebViewActivity" />
        <activity android:name=".activity.Brand_itemActivity" />
        <activity android:name=".activity.GoodsInfoActivity" />
        <activity android:name=".activity.DarRenActivity" />
        <activity
            android:name=".activity.PaiXuActivity"
            android:theme="@style/MuCusntmerStyle" />
        <activity android:name=".activity.DarenActivity2" />
        <activity android:name=".activity.ShouHouActivity" />
        <activity android:name=".activity.GoodsSelectActivity" />
        <activity android:name=".activity.ShowImageAndGifActivity" />
        <activity android:name=".activity.GifAndImageActivity" />
        //shared sdk
        <activity
            android:name="com.mob.tools.MobUIShell"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <data android:scheme="tencent100371282" />

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 调用新浪原生SDK，需要注册的回调activity -->
            <intent-filter>
                <action android:name="com.sina.weibo.sdk.action.ACTION_SDK_REQ_ACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 集成line客户端登录授权，需要添如下格式的过滤器 -->
            <intent-filter>
                <data android:scheme="line.1477692153" />

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 微信分享回调 -->
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".activity.ZaZhiActivity" />
        <activity android:name=".activity.ZaZhiItemActivity" />
        <activity android:name=".activity.ShopngCarActivity" />
        <activity android:name=".activity.DaRenRecommonPingLunInfoActivity" />
        <activity android:name=".carstorage.ShoppingCarActivity" />
        <activity android:name=".activity.DingDanActivity"></activity>
    </application>

</manifest>